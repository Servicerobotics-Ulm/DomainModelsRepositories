//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMTRACKINGOBJECTS_PERSONLOSTEVENTTYPE_ENUM_H_
#define COMMTRACKINGOBJECTS_PERSONLOSTEVENTTYPE_ENUM_H_

#include <CommTrackingObjects/enumPersonLostEventTypeData.hh>

#include <string>
#include <iostream>
#include <locale>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommTrackingObjects {
	
	class PersonLostEventType {
	private:
		CommTrackingObjectsIDL::PersonLostEventType value;
		
	public:
		enum ENUM_PersonLostEventType {
			ENUM_VALUE_UNDEFINED = 0,
			PERSON_LOST = 1,
			PERSON_FOUND = 2,
			PERSON_LOST_CROWD = 3
		};
		
		// default constructor
		PersonLostEventType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		PersonLostEventType(ENUM_PersonLostEventType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type (which is typically int)
		PersonLostEventType(CommTrackingObjectsIDL::PersonLostEventType e) {
			value = e;
		}
		
		//const CommTrackingObjectsIDL::PersonLostEventType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_PersonLostEventType() const {
			return static_cast<ENUM_PersonLostEventType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_PersonLostEventType t) const {
			return this->value == t;
		}
		
		std::string to_string(const bool &use_fqn=true) const {
			std::string result = "";
			if(use_fqn == true) {
				result = "PersonLostEventType::";
			}
			switch (value) {
				case PERSON_LOST:
					result += "PERSON_LOST";
					break;
				case PERSON_FOUND:
					result += "PERSON_FOUND";
					break;
				case PERSON_LOST_CROWD:
					result += "PERSON_LOST_CROWD";
					break;
				default:
					result += "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		static PersonLostEventType from_string(const std::string &value) {
			std::string input = value;
			std::locale l;
			for(auto &c: input) {
				// convert all characters to lower case (so string comparison works regardless of small/capital letters)
				c = std::tolower(c,l);
			}
			std::string base_name = "personlosteventtype::";
			if(input.compare(0, base_name.length(), base_name) == 0) {
				// remove basename from comparing the actual enumeration
				input.erase(0,base_name.length());
			}
			if(input == "person_lost"){
				return PersonLostEventType(PERSON_LOST);
			}
			if(input == "person_found"){
				return PersonLostEventType(PERSON_FOUND);
			}
			if(input == "person_lost_crowd"){
				return PersonLostEventType(PERSON_LOST_CROWD);
			}
			// default (if none of the preceding options match)
			return PersonLostEventType();
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const PersonLostEventType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
