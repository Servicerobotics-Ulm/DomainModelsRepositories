//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMTRACKINGOBJECTS_LEADEREVENTTYPE_ENUM_H_
#define COMMTRACKINGOBJECTS_LEADEREVENTTYPE_ENUM_H_

#include <CommTrackingObjects/enumLeaderEventTypeData.hh>

#include <string>
#include <iostream>
#include <locale>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommTrackingObjects {
	
	class LeaderEventType {
	private:
		CommTrackingObjectsIDL::LeaderEventType value;
		
	public:
		enum ENUM_LeaderEventType {
			ENUM_VALUE_UNDEFINED = 0,
			TOO_CLOSE = 1,
			LEADER_IMAGE_SUCCESS = 2,
			LEADER_FOUND = 3,
			TOO_MANY_PERSONS = 4,
			NO_FACES_FOUND = 5,
			TOO_FEW_HANDS = 6,
			WAVE_DETECTED = 7,
			UNKNOWN = 8
		};
		
		// default constructor
		LeaderEventType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		LeaderEventType(ENUM_LeaderEventType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type (which is typically int)
		LeaderEventType(CommTrackingObjectsIDL::LeaderEventType e) {
			value = e;
		}
		
		//const CommTrackingObjectsIDL::LeaderEventType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_LeaderEventType() const {
			return static_cast<ENUM_LeaderEventType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_LeaderEventType t) const {
			return this->value == t;
		}
		
		std::string to_string(const bool &use_fqn=true) const {
			std::string result = "";
			if(use_fqn == true) {
				result = "LeaderEventType::";
			}
			switch (value) {
				case TOO_CLOSE:
					result += "TOO_CLOSE";
					break;
				case LEADER_IMAGE_SUCCESS:
					result += "LEADER_IMAGE_SUCCESS";
					break;
				case LEADER_FOUND:
					result += "LEADER_FOUND";
					break;
				case TOO_MANY_PERSONS:
					result += "TOO_MANY_PERSONS";
					break;
				case NO_FACES_FOUND:
					result += "NO_FACES_FOUND";
					break;
				case TOO_FEW_HANDS:
					result += "TOO_FEW_HANDS";
					break;
				case WAVE_DETECTED:
					result += "WAVE_DETECTED";
					break;
				case UNKNOWN:
					result += "UNKNOWN";
					break;
				default:
					result += "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		static LeaderEventType from_string(const std::string &value) {
			std::string input = value;
			std::locale l;
			for(auto &c: input) {
				// convert all characters to lower case (so string comparison works regardless of small/capital letters)
				c = std::tolower(c,l);
			}
			std::string base_name = "leadereventtype::";
			if(input.compare(0, base_name.length(), base_name) == 0) {
				// remove basename from comparing the actual enumeration
				input.erase(0,base_name.length());
			}
			if(input == "too_close"){
				return LeaderEventType(TOO_CLOSE);
			}
			if(input == "leader_image_success"){
				return LeaderEventType(LEADER_IMAGE_SUCCESS);
			}
			if(input == "leader_found"){
				return LeaderEventType(LEADER_FOUND);
			}
			if(input == "too_many_persons"){
				return LeaderEventType(TOO_MANY_PERSONS);
			}
			if(input == "no_faces_found"){
				return LeaderEventType(NO_FACES_FOUND);
			}
			if(input == "too_few_hands"){
				return LeaderEventType(TOO_FEW_HANDS);
			}
			if(input == "wave_detected"){
				return LeaderEventType(WAVE_DETECTED);
			}
			if(input == "unknown"){
				return LeaderEventType(UNKNOWN);
			}
			// default (if none of the preceding options match)
			return LeaderEventType();
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const LeaderEventType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
