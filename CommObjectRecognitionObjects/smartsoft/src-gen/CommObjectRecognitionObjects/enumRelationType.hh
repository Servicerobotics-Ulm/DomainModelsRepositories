//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMOBJECTRECOGNITIONOBJECTS_RELATIONTYPE_ENUM_H_
#define COMMOBJECTRECOGNITIONOBJECTS_RELATIONTYPE_ENUM_H_

#include <CommObjectRecognitionObjects/enumRelationTypeData.hh>

#include <string>
#include <iostream>
#include <locale>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommObjectRecognitionObjects {
	
	class RelationType {
	private:
		CommObjectRecognitionObjectsIDL::RelationType value;
		
	public:
		enum ENUM_RelationType {
			ENUM_VALUE_UNDEFINED = 0,
			ON_TOP_OF = 1,
			BELOW_OF = 2
		};
		
		// default constructor
		RelationType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		RelationType(ENUM_RelationType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type (which is typically int)
		RelationType(CommObjectRecognitionObjectsIDL::RelationType e) {
			value = e;
		}
		
		//const CommObjectRecognitionObjectsIDL::RelationType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_RelationType() const {
			return static_cast<ENUM_RelationType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_RelationType t) const {
			return this->value == t;
		}
		
		std::string to_string(const bool &use_fqn=true) const {
			std::string result = "";
			if(use_fqn == true) {
				result = "RelationType::";
			}
			switch (value) {
				case ON_TOP_OF:
					result += "ON_TOP_OF";
					break;
				case BELOW_OF:
					result += "BELOW_OF";
					break;
				default:
					result += "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		static RelationType from_string(const std::string &value) {
			std::string input = value;
			std::locale l;
			for(auto &c: input) {
				// convert all characters to lower case (so string comparison works regardless of small/capital letters)
				c = std::tolower(c,l);
			}
			std::string base_name = "relationtype::";
			if(input.compare(0, base_name.length(), base_name) == 0) {
				// remove basename from comparing the actual enumeration
				input.erase(0,base_name.length());
			}
			if(input == "on_top_of"){
				return RelationType(ON_TOP_OF);
			}
			if(input == "below_of"){
				return RelationType(BELOW_OF);
			}
			// default (if none of the preceding options match)
			return RelationType();
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const RelationType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
