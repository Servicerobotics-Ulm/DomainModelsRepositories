//-----------------------------------------------------------------------------------
// Code generated by the Toolify Robotics GmbH extension for the SmartMDSD Toolchain 
// 
// Toolify Robotics GmbH
// Weinbergweg 216
// 89075 Ulm 
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//-----------------------------------------------------------------------------------

#include "CommImageObjectRecognitionResultsJSON.hh"

#include "CommObjectRecognitionObjectsJSON/CommImageObjectRecognitionResultJSON.hh"
#include "CommBasicObjectsJSON/CommTimeStampJSON.hh"

namespace CommObjectRecognitionObjectsIDL {

void to_json(const CommObjectRecognitionObjectsIDL::CommImageObjectRecognitionResults& obj, nlohmann::json& j)
{
	// objects: CommImageObjectRecognitionResult[]
	for(size_t idx=0; idx < obj.objects.size(); idx++) {
		nlohmann::json array_element;
		to_json(obj.objects.at(idx), array_element);
		j["objects"].emplace_back(array_element);
	}
	// timestamp: CommTimeStamp
	to_json(obj.timestamp, j["timestamp"]);
	// environmentId: UInt32
	j["environmentId"] = obj.environmentId;
	// valid: Boolean
	j["valid"] = obj.valid;
}

/**
 * this transformation handles missing values and checks for correct types:
 * - missing primitive type values (including simple arrays) are set to zero values
 * - missing object type values are skipped entirely
 * - types are checked before performing the type-cast operation
 */
void from_json(const nlohmann::json& j, CommObjectRecognitionObjectsIDL::CommImageObjectRecognitionResults& obj)
{
	// objects: CommImageObjectRecognitionResult[]
	if(j.contains("objects") && j["objects"].is_array()) {
		auto jobjects = j["objects"];
		obj.objects.resize(jobjects.size());
		for(size_t idx=0; idx < jobjects.size(); idx++) {
			// convert the json array values individually
			if(jobjects.at(idx).is_object()) {
				// from_json(jobjects.at(idx), obj.objects.at(idx));
				obj.objects[idx] = jobjects[idx].get<CommObjectRecognitionObjectsIDL::CommImageObjectRecognitionResult>();
			}
		}
	}
	// timestamp: CommTimeStamp
	if(j.contains("timestamp") && j["timestamp"].is_object()) {
		//from_json(j["timestamp"], obj.timestamp);
		obj.timestamp = j["timestamp"].get<CommBasicObjectsIDL::CommTimeStamp>();
	}
	// environmentId: UInt32
	if(j.contains("environmentId") && j["environmentId"].is_number_unsigned()) {
		obj.environmentId = j["environmentId"].get<unsigned int>();
	}
	// valid: Boolean
	if(j.contains("valid") && j["valid"].is_boolean()) {
		obj.valid = j["valid"].get<bool>();
	}
}

} // end namespace CommObjectRecognitionObjectsIDL
