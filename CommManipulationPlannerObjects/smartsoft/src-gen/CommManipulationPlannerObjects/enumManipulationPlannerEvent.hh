//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMMANIPULATIONPLANNEROBJECTS_MANIPULATIONPLANNEREVENT_ENUM_H_
#define COMMMANIPULATIONPLANNEROBJECTS_MANIPULATIONPLANNEREVENT_ENUM_H_

#include <CommManipulationPlannerObjects/enumManipulationPlannerEventData.hh>

#include <string>
#include <iostream>
#include <locale>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommManipulationPlannerObjects {
	
	class ManipulationPlannerEvent {
	private:
		CommManipulationPlannerObjectsIDL::ManipulationPlannerEvent value;
		
	public:
		enum ENUM_ManipulationPlannerEvent {
			ENUM_VALUE_UNDEFINED = 0,
			MANIPULATOR_SYNC_FAIL = 1,
			NO_IK_SOLUTION_FOUND = 2,
			NO_PATH_FOUND = 3,
			PATH_FOUND = 4,
			PLANNING_PATH = 5,
			LOAD_OBJREC_DATA_DONE = 6,
			UNKNOWN = 7
		};
		
		// default constructor
		ManipulationPlannerEvent() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		ManipulationPlannerEvent(ENUM_ManipulationPlannerEvent e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type (which is typically int)
		ManipulationPlannerEvent(CommManipulationPlannerObjectsIDL::ManipulationPlannerEvent e) {
			value = e;
		}
		
		//const CommManipulationPlannerObjectsIDL::ManipulationPlannerEvent& get() const { return value; }
		
		// ENUM operator
		operator ENUM_ManipulationPlannerEvent() const {
			return static_cast<ENUM_ManipulationPlannerEvent>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_ManipulationPlannerEvent t) const {
			return this->value == t;
		}
		
		std::string to_string(const bool &use_fqn=true) const {
			std::string result = "";
			if(use_fqn == true) {
				result = "ManipulationPlannerEvent::";
			}
			switch (value) {
				case MANIPULATOR_SYNC_FAIL:
					result += "MANIPULATOR_SYNC_FAIL";
					break;
				case NO_IK_SOLUTION_FOUND:
					result += "NO_IK_SOLUTION_FOUND";
					break;
				case NO_PATH_FOUND:
					result += "NO_PATH_FOUND";
					break;
				case PATH_FOUND:
					result += "PATH_FOUND";
					break;
				case PLANNING_PATH:
					result += "PLANNING_PATH";
					break;
				case LOAD_OBJREC_DATA_DONE:
					result += "LOAD_OBJREC_DATA_DONE";
					break;
				case UNKNOWN:
					result += "UNKNOWN";
					break;
				default:
					result += "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		static ManipulationPlannerEvent from_string(const std::string &value) {
			std::string input = value;
			std::locale l;
			for(auto &c: input) {
				// convert all characters to lower case (so string comparison works regardless of small/capital letters)
				c = std::tolower(c,l);
			}
			std::string base_name = "manipulationplannerevent::";
			if(input.compare(0, base_name.length(), base_name) == 0) {
				// remove basename from comparing the actual enumeration
				input.erase(0,base_name.length());
			}
			if(input == "manipulator_sync_fail"){
				return ManipulationPlannerEvent(MANIPULATOR_SYNC_FAIL);
			}
			if(input == "no_ik_solution_found"){
				return ManipulationPlannerEvent(NO_IK_SOLUTION_FOUND);
			}
			if(input == "no_path_found"){
				return ManipulationPlannerEvent(NO_PATH_FOUND);
			}
			if(input == "path_found"){
				return ManipulationPlannerEvent(PATH_FOUND);
			}
			if(input == "planning_path"){
				return ManipulationPlannerEvent(PLANNING_PATH);
			}
			if(input == "load_objrec_data_done"){
				return ManipulationPlannerEvent(LOAD_OBJREC_DATA_DONE);
			}
			if(input == "unknown"){
				return ManipulationPlannerEvent(UNKNOWN);
			}
			// default (if none of the preceding options match)
			return ManipulationPlannerEvent();
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const ManipulationPlannerEvent &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
