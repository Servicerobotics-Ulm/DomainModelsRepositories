//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMMANIPULATOROBJECTS_GRIPPEREVENT_ENUM_H_
#define COMMMANIPULATOROBJECTS_GRIPPEREVENT_ENUM_H_

#include <CommManipulatorObjects/enumGripperEventData.hh>

#include <string>
#include <iostream>
#include <locale>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommManipulatorObjects {
	
	class GripperEvent {
	private:
		CommManipulatorObjectsIDL::GripperEvent value;
		
	public:
		enum ENUM_GripperEvent {
			ENUM_VALUE_UNDEFINED = 0,
			GRIPPER_GOAL_REACHED = 1,
			GRIPPER_GOAL_NOT_REACHED = 2,
			GRIPPER_GOAL_REACHED_EMPTY = 3,
			GRIPPER_UNKNOWN = 4
		};
		
		// default constructor
		GripperEvent() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		GripperEvent(ENUM_GripperEvent e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type (which is typically int)
		GripperEvent(CommManipulatorObjectsIDL::GripperEvent e) {
			value = e;
		}
		
		//const CommManipulatorObjectsIDL::GripperEvent& get() const { return value; }
		
		// ENUM operator
		operator ENUM_GripperEvent() const {
			return static_cast<ENUM_GripperEvent>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_GripperEvent t) const {
			return this->value == t;
		}
		
		std::string to_string(const bool &use_fqn=true) const {
			std::string result = "";
			if(use_fqn == true) {
				result = "GripperEvent::";
			}
			switch (value) {
				case GRIPPER_GOAL_REACHED:
					result += "GRIPPER_GOAL_REACHED";
					break;
				case GRIPPER_GOAL_NOT_REACHED:
					result += "GRIPPER_GOAL_NOT_REACHED";
					break;
				case GRIPPER_GOAL_REACHED_EMPTY:
					result += "GRIPPER_GOAL_REACHED_EMPTY";
					break;
				case GRIPPER_UNKNOWN:
					result += "GRIPPER_UNKNOWN";
					break;
				default:
					result += "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		static GripperEvent from_string(const std::string &value) {
			std::string input = value;
			std::locale l;
			for(auto &c: input) {
				// convert all characters to lower case (so string comparison works regardless of small/capital letters)
				c = std::tolower(c,l);
			}
			std::string base_name = "gripperevent::";
			if(input.compare(0, base_name.length(), base_name) == 0) {
				// remove basename from comparing the actual enumeration
				input.erase(0,base_name.length());
			}
			if(input == "gripper_goal_reached"){
				return GripperEvent(GRIPPER_GOAL_REACHED);
			}
			if(input == "gripper_goal_not_reached"){
				return GripperEvent(GRIPPER_GOAL_NOT_REACHED);
			}
			if(input == "gripper_goal_reached_empty"){
				return GripperEvent(GRIPPER_GOAL_REACHED_EMPTY);
			}
			if(input == "gripper_unknown"){
				return GripperEvent(GRIPPER_UNKNOWN);
			}
			// default (if none of the preceding options match)
			return GripperEvent();
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const GripperEvent &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
