//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "CommManipulatorObjects/CommManipulationTrajectoryCore.hh"

// serialization/deserialization operators
//#include "CommManipulatorObjects/CommManipulationTrajectoryACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace CommManipulatorObjects 
{
	const char* CommManipulationTrajectoryCore::getCompiledHash()
	{
		return CommManipulatorObjectsIDL::REPO_HASH;
	}
	
	void CommManipulationTrajectoryCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for CommManipulatorObjects::CommJointManipulationTarget(idl_CommManipulationTrajectory.joint_angles)
		CommManipulatorObjects::CommJointManipulationTarget::getAllHashValues(hashes);
		// get hash value(s) for CommManipulatorObjects::CommCartesianManipulationTarget(idl_CommManipulationTrajectory.tcp_poses)
		CommManipulatorObjects::CommCartesianManipulationTarget::getAllHashValues(hashes);
	}
	
	void CommManipulationTrajectoryCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "CommManipulationTrajectoryCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for CommManipulatorObjects::CommJointManipulationTarget(idl_CommManipulationTrajectory.joint_angles)
		CommManipulatorObjects::CommJointManipulationTarget::checkAllHashValues(hashes);
		// check hash value(s) for CommManipulatorObjects::CommCartesianManipulationTarget(idl_CommManipulationTrajectory.tcp_poses)
		CommManipulatorObjects::CommCartesianManipulationTarget::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t CommManipulationTrajectoryCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		std::vector<CommManipulatorObjectsIDL::CommJointManipulationTarget>::const_iterator data_joint_anglesIt;
		for(data_joint_anglesIt=data.joint_angles.begin(); data_joint_anglesIt!=data.joint_angles.end(); data_joint_anglesIt++) {
			seed += CommManipulatorObjects::CommJointManipulationTarget::generateDataHash(*data_joint_anglesIt);
		}
		std::vector<CommManipulatorObjectsIDL::CommCartesianManipulationTarget>::const_iterator data_tcp_posesIt;
		for(data_tcp_posesIt=data.tcp_poses.begin(); data_tcp_posesIt!=data.tcp_poses.end(); data_tcp_posesIt++) {
			seed += CommManipulatorObjects::CommCartesianManipulationTarget::generateDataHash(*data_tcp_posesIt);
		}
		boost::hash_combine(seed, data.type);
		
		return seed;
	}
	#endif
	
	// default constructor
	CommManipulationTrajectoryCore::CommManipulationTrajectoryCore()
	:	idl_CommManipulationTrajectory()
	{  
		setJoint_angles(std::vector<CommManipulatorObjects::CommJointManipulationTarget>());
		setTcp_poses(std::vector<CommManipulatorObjects::CommCartesianManipulationTarget>());
		setType(CommManipulatorObjects::ManipulatorTrajectoryFlag());
	}
	
	CommManipulationTrajectoryCore::CommManipulationTrajectoryCore(const DATATYPE &data)
	:	idl_CommManipulationTrajectory(data)
	{  }

	CommManipulationTrajectoryCore::~CommManipulationTrajectoryCore()
	{  }
	
	void CommManipulationTrajectoryCore::to_ostream(std::ostream &os) const
	{
	  os << "CommManipulationTrajectory(";
	  std::vector<CommManipulatorObjects::CommJointManipulationTarget>::const_iterator joint_anglesIt;
	  std::vector<CommManipulatorObjects::CommJointManipulationTarget> joint_anglesList = getJoint_anglesCopy();
	  for(joint_anglesIt=joint_anglesList.begin(); joint_anglesIt!=joint_anglesList.end(); joint_anglesIt++) {
	  	joint_anglesIt->to_ostream(os);
	  }
	  std::vector<CommManipulatorObjects::CommCartesianManipulationTarget>::const_iterator tcp_posesIt;
	  std::vector<CommManipulatorObjects::CommCartesianManipulationTarget> tcp_posesList = getTcp_posesCopy();
	  for(tcp_posesIt=tcp_posesList.begin(); tcp_posesIt!=tcp_posesList.end(); tcp_posesIt++) {
	  	tcp_posesIt->to_ostream(os);
	  }
	  getType().to_ostream(os);
	  os << ") ";
	}
	
	// convert to xml stream
	void CommManipulationTrajectoryCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		std::vector<CommManipulatorObjects::CommJointManipulationTarget>::const_iterator joint_anglesIt;
		std::vector<CommManipulatorObjects::CommJointManipulationTarget> joint_anglesList = getJoint_anglesCopy();
		counter = 0;
		os << indent << "<joint_anglesList n=\"" << joint_anglesList.size() << "\">";
		for(joint_anglesIt=joint_anglesList.begin(); joint_anglesIt!=joint_anglesList.end(); joint_anglesIt++) {
			os << indent << "<joint_angles i=\"" << counter++ << "\">";
			joint_anglesIt->to_xml(os, indent);
			os << indent << "</joint_angles>";
		}
		os << indent << "</joint_anglesList>";
		std::vector<CommManipulatorObjects::CommCartesianManipulationTarget>::const_iterator tcp_posesIt;
		std::vector<CommManipulatorObjects::CommCartesianManipulationTarget> tcp_posesList = getTcp_posesCopy();
		counter = 0;
		os << indent << "<tcp_posesList n=\"" << tcp_posesList.size() << "\">";
		for(tcp_posesIt=tcp_posesList.begin(); tcp_posesIt!=tcp_posesList.end(); tcp_posesIt++) {
			os << indent << "<tcp_poses i=\"" << counter++ << "\">";
			tcp_posesIt->to_xml(os, indent);
			os << indent << "</tcp_poses>";
		}
		os << indent << "</tcp_posesList>";
		os << indent << "<type>";
		getType().to_xml(os, indent);
		os << indent << "</type>";
	}
	
	// restore from xml stream
	void CommManipulationTrajectoryCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_joint_anglesList("<joint_anglesList n=\"");
		static const Smart::KnuthMorrisPratt kmp_joint_angles("\">");
		static const Smart::KnuthMorrisPratt kmp_tcp_posesList("<tcp_posesList n=\"");
		static const Smart::KnuthMorrisPratt kmp_tcp_poses("\">");
		static const Smart::KnuthMorrisPratt kmp_type("<type>");
		
		if(kmp_joint_anglesList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			CommManipulatorObjects::CommJointManipulationTarget joint_anglesItem;
			std::vector<CommManipulatorObjects::CommJointManipulationTarget> joint_anglesList;
			kmp_joint_angles.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_joint_angles.search(is)) {
					joint_anglesItem.from_xml(is);
					joint_anglesList.push_back(joint_anglesItem);
				}
			}
			setJoint_angles(joint_anglesList);
		}
		if(kmp_tcp_posesList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			CommManipulatorObjects::CommCartesianManipulationTarget tcp_posesItem;
			std::vector<CommManipulatorObjects::CommCartesianManipulationTarget> tcp_posesList;
			kmp_tcp_poses.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_tcp_poses.search(is)) {
					tcp_posesItem.from_xml(is);
					tcp_posesList.push_back(tcp_posesItem);
				}
			}
			setTcp_poses(tcp_posesList);
		}
		if(kmp_type.search(is)) {
			CommManipulatorObjects::ManipulatorTrajectoryFlag typeItem;
			typeItem.from_xml(is);
			setType(typeItem);
		}
	}
	
	/*
	void CommManipulationTrajectoryCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		CommManipulatorObjectsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(CommManipulatorObjectsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see CommManipulationTrajectoryACE.hh)
		cdr << idl_CommManipulationTrajectory;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_CommManipulationTrajectory);
		cdr << data_hash;
		// std::cout << "CommManipulationTrajectoryCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void CommManipulationTrajectoryCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		CommManipulatorObjectsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see CommManipulationTrajectoryACE.hh)
		cdr >> idl_CommManipulationTrajectory;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_CommManipulationTrajectory);
		assert(data_hash == own_hash);
		// std::cout << "CommManipulationTrajectoryCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace CommManipulatorObjects */
