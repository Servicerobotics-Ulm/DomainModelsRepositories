//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMMANIPULATOROBJECTS_MANIPULATOREVENT_ENUM_H_
#define COMMMANIPULATOROBJECTS_MANIPULATOREVENT_ENUM_H_

#include <CommManipulatorObjects/enumManipulatorEventData.hh>

#include <string>
#include <iostream>
#include <locale>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommManipulatorObjects {
	
	class ManipulatorEvent {
	private:
		CommManipulatorObjectsIDL::ManipulatorEvent value;
		
	public:
		enum ENUM_ManipulatorEvent {
			ENUM_VALUE_UNDEFINED = 0,
			COLLISION = 1,
			NO_SOLUTION_FOUND = 2,
			VALUE_OUT_OF_RANGE = 3,
			GOAL_REACHED = 4,
			GOAL_NOT_REACHED = 5,
			INTERMEDIATE_GOAL_REACHED = 6,
			CANCELLED = 7,
			ERROR = 8,
			UNKNOWN = 9
		};
		
		// default constructor
		ManipulatorEvent() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		ManipulatorEvent(ENUM_ManipulatorEvent e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type (which is typically int)
		ManipulatorEvent(CommManipulatorObjectsIDL::ManipulatorEvent e) {
			value = e;
		}
		
		//const CommManipulatorObjectsIDL::ManipulatorEvent& get() const { return value; }
		
		// ENUM operator
		operator ENUM_ManipulatorEvent() const {
			return static_cast<ENUM_ManipulatorEvent>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_ManipulatorEvent t) const {
			return this->value == t;
		}
		
		std::string to_string(const bool &use_fqn=true) const {
			std::string result = "";
			if(use_fqn == true) {
				result = "ManipulatorEvent::";
			}
			switch (value) {
				case COLLISION:
					result += "COLLISION";
					break;
				case NO_SOLUTION_FOUND:
					result += "NO_SOLUTION_FOUND";
					break;
				case VALUE_OUT_OF_RANGE:
					result += "VALUE_OUT_OF_RANGE";
					break;
				case GOAL_REACHED:
					result += "GOAL_REACHED";
					break;
				case GOAL_NOT_REACHED:
					result += "GOAL_NOT_REACHED";
					break;
				case INTERMEDIATE_GOAL_REACHED:
					result += "INTERMEDIATE_GOAL_REACHED";
					break;
				case CANCELLED:
					result += "CANCELLED";
					break;
				case ERROR:
					result += "ERROR";
					break;
				case UNKNOWN:
					result += "UNKNOWN";
					break;
				default:
					result += "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		static ManipulatorEvent from_string(const std::string &value) {
			std::string input = value;
			std::locale l;
			for(auto &c: input) {
				// convert all characters to lower case (so string comparison works regardless of small/capital letters)
				c = std::tolower(c,l);
			}
			std::string base_name = "manipulatorevent::";
			if(input.compare(0, base_name.length(), base_name) == 0) {
				// remove basename from comparing the actual enumeration
				input.erase(0,base_name.length());
			}
			if(input == "collision"){
				return ManipulatorEvent(COLLISION);
			}
			if(input == "no_solution_found"){
				return ManipulatorEvent(NO_SOLUTION_FOUND);
			}
			if(input == "value_out_of_range"){
				return ManipulatorEvent(VALUE_OUT_OF_RANGE);
			}
			if(input == "goal_reached"){
				return ManipulatorEvent(GOAL_REACHED);
			}
			if(input == "goal_not_reached"){
				return ManipulatorEvent(GOAL_NOT_REACHED);
			}
			if(input == "intermediate_goal_reached"){
				return ManipulatorEvent(INTERMEDIATE_GOAL_REACHED);
			}
			if(input == "cancelled"){
				return ManipulatorEvent(CANCELLED);
			}
			if(input == "error"){
				return ManipulatorEvent(ERROR);
			}
			if(input == "unknown"){
				return ManipulatorEvent(UNKNOWN);
			}
			// default (if none of the preceding options match)
			return ManipulatorEvent();
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const ManipulatorEvent &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
