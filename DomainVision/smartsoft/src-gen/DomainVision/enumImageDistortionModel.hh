//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef DOMAINVISION_IMAGEDISTORTIONMODEL_ENUM_H_
#define DOMAINVISION_IMAGEDISTORTIONMODEL_ENUM_H_

#include <DomainVision/enumImageDistortionModelData.hh>

#include <string>
#include <iostream>
#include <locale>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace DomainVision {
	
	class ImageDistortionModel {
	private:
		DomainVisionIDL::ImageDistortionModel value;
		
	public:
		enum ENUM_ImageDistortionModel {
			ENUM_VALUE_UNDEFINED = 0,
			NONE = 1,
			MODIFIED_BROWN_CONRADY = 2,
			INVERSE_BROWN_CONRADY = 3,
			BROWN_CONRADY = 4
		};
		
		// default constructor
		ImageDistortionModel() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		ImageDistortionModel(ENUM_ImageDistortionModel e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type (which is typically int)
		ImageDistortionModel(DomainVisionIDL::ImageDistortionModel e) {
			value = e;
		}
		
		//const DomainVisionIDL::ImageDistortionModel& get() const { return value; }
		
		// ENUM operator
		operator ENUM_ImageDistortionModel() const {
			return static_cast<ENUM_ImageDistortionModel>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_ImageDistortionModel t) const {
			return this->value == t;
		}
		
		std::string to_string(const bool &use_fqn=true) const {
			std::string result = "";
			if(use_fqn == true) {
				result = "ImageDistortionModel::";
			}
			switch (value) {
				case NONE:
					result += "NONE";
					break;
				case MODIFIED_BROWN_CONRADY:
					result += "MODIFIED_BROWN_CONRADY";
					break;
				case INVERSE_BROWN_CONRADY:
					result += "INVERSE_BROWN_CONRADY";
					break;
				case BROWN_CONRADY:
					result += "BROWN_CONRADY";
					break;
				default:
					result += "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		static ImageDistortionModel from_string(const std::string &value) {
			std::string input = value;
			std::locale l;
			for(auto &c: input) {
				// convert all characters to lower case (so string comparison works regardless of small/capital letters)
				c = std::tolower(c,l);
			}
			std::string base_name = "imagedistortionmodel::";
			if(input.compare(0, base_name.length(), base_name) == 0) {
				// remove basename from comparing the actual enumeration
				input.erase(0,base_name.length());
			}
			if(input == "none"){
				return ImageDistortionModel(NONE);
			}
			if(input == "modified_brown_conrady"){
				return ImageDistortionModel(MODIFIED_BROWN_CONRADY);
			}
			if(input == "inverse_brown_conrady"){
				return ImageDistortionModel(INVERSE_BROWN_CONRADY);
			}
			if(input == "brown_conrady"){
				return ImageDistortionModel(BROWN_CONRADY);
			}
			// default (if none of the preceding options match)
			return ImageDistortionModel();
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const ImageDistortionModel &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
