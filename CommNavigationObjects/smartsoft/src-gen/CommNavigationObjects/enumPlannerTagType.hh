//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMNAVIGATIONOBJECTS_PLANNERTAGTYPE_ENUM_H_
#define COMMNAVIGATIONOBJECTS_PLANNERTAGTYPE_ENUM_H_

#include <CommNavigationObjects/enumPlannerTagTypeData.hh>

#include <string>
#include <iostream>
#include <locale>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommNavigationObjects {
	
	class PlannerTagType {
	private:
		CommNavigationObjectsIDL::PlannerTagType value;
		
	public:
		enum ENUM_PlannerTagType {
			ENUM_VALUE_UNDEFINED = 0,
			PLANNER_DELETE_GOAL = 1,
			PLANNER_SET_DESTINATION_LINE = 2,
			PLANNER_SET_DESTINATION_CIRCLE = 3,
			PLANNER_ID = 4
		};
		
		// default constructor
		PlannerTagType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		PlannerTagType(ENUM_PlannerTagType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type (which is typically int)
		PlannerTagType(CommNavigationObjectsIDL::PlannerTagType e) {
			value = e;
		}
		
		//const CommNavigationObjectsIDL::PlannerTagType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_PlannerTagType() const {
			return static_cast<ENUM_PlannerTagType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_PlannerTagType t) const {
			return this->value == t;
		}
		
		std::string to_string(const bool &use_fqn=true) const {
			std::string result = "";
			if(use_fqn == true) {
				result = "PlannerTagType::";
			}
			switch (value) {
				case PLANNER_DELETE_GOAL:
					result += "PLANNER_DELETE_GOAL";
					break;
				case PLANNER_SET_DESTINATION_LINE:
					result += "PLANNER_SET_DESTINATION_LINE";
					break;
				case PLANNER_SET_DESTINATION_CIRCLE:
					result += "PLANNER_SET_DESTINATION_CIRCLE";
					break;
				case PLANNER_ID:
					result += "PLANNER_ID";
					break;
				default:
					result += "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		static PlannerTagType from_string(const std::string &value) {
			std::string input = value;
			std::locale l;
			for(auto &c: input) {
				// convert all characters to lower case (so string comparison works regardless of small/capital letters)
				c = std::tolower(c,l);
			}
			std::string base_name = "plannertagtype::";
			if(input.compare(0, base_name.length(), base_name) == 0) {
				// remove basename from comparing the actual enumeration
				input.erase(0,base_name.length());
			}
			if(input == "planner_delete_goal"){
				return PlannerTagType(PLANNER_DELETE_GOAL);
			}
			if(input == "planner_set_destination_line"){
				return PlannerTagType(PLANNER_SET_DESTINATION_LINE);
			}
			if(input == "planner_set_destination_circle"){
				return PlannerTagType(PLANNER_SET_DESTINATION_CIRCLE);
			}
			if(input == "planner_id"){
				return PlannerTagType(PLANNER_ID);
			}
			// default (if none of the preceding options match)
			return PlannerTagType();
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const PlannerTagType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
