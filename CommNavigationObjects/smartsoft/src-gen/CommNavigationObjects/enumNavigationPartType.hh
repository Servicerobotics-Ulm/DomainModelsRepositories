//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMNAVIGATIONOBJECTS_NAVIGATIONPARTTYPE_ENUM_H_
#define COMMNAVIGATIONOBJECTS_NAVIGATIONPARTTYPE_ENUM_H_

#include <CommNavigationObjects/enumNavigationPartTypeData.hh>

#include <string>
#include <iostream>
#include <locale>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommNavigationObjects {
	
	class NavigationPartType {
	private:
		CommNavigationObjectsIDL::NavigationPartType value;
		
	public:
		enum ENUM_NavigationPartType {
			ENUM_VALUE_UNDEFINED = 0,
			GRID_BASED_FREE_NAV = 1,
			CORRIDOR_BASED_COORDINATED_NAV = 2,
			GRID_BASED_COORDINATED_NAV = 3
		};
		
		// default constructor
		NavigationPartType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		NavigationPartType(ENUM_NavigationPartType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type (which is typically int)
		NavigationPartType(CommNavigationObjectsIDL::NavigationPartType e) {
			value = e;
		}
		
		//const CommNavigationObjectsIDL::NavigationPartType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_NavigationPartType() const {
			return static_cast<ENUM_NavigationPartType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_NavigationPartType t) const {
			return this->value == t;
		}
		
		std::string to_string(const bool &use_fqn=true) const {
			std::string result = "";
			if(use_fqn == true) {
				result = "NavigationPartType::";
			}
			switch (value) {
				case GRID_BASED_FREE_NAV:
					result += "GRID_BASED_FREE_NAV";
					break;
				case CORRIDOR_BASED_COORDINATED_NAV:
					result += "CORRIDOR_BASED_COORDINATED_NAV";
					break;
				case GRID_BASED_COORDINATED_NAV:
					result += "GRID_BASED_COORDINATED_NAV";
					break;
				default:
					result += "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		static NavigationPartType from_string(const std::string &value) {
			std::string input = value;
			std::locale l;
			for(auto &c: input) {
				// convert all characters to lower case (so string comparison works regardless of small/capital letters)
				c = std::tolower(c,l);
			}
			std::string base_name = "navigationparttype::";
			if(input.compare(0, base_name.length(), base_name) == 0) {
				// remove basename from comparing the actual enumeration
				input.erase(0,base_name.length());
			}
			if(input == "grid_based_free_nav"){
				return NavigationPartType(GRID_BASED_FREE_NAV);
			}
			if(input == "corridor_based_coordinated_nav"){
				return NavigationPartType(CORRIDOR_BASED_COORDINATED_NAV);
			}
			if(input == "grid_based_coordinated_nav"){
				return NavigationPartType(GRID_BASED_COORDINATED_NAV);
			}
			// default (if none of the preceding options match)
			return NavigationPartType();
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const NavigationPartType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
