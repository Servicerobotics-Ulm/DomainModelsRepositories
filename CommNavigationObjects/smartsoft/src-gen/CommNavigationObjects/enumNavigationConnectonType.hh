//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMNAVIGATIONOBJECTS_NAVIGATIONCONNECTONTYPE_ENUM_H_
#define COMMNAVIGATIONOBJECTS_NAVIGATIONCONNECTONTYPE_ENUM_H_

#include <CommNavigationObjects/enumNavigationConnectonTypeData.hh>

#include <string>
#include <iostream>
#include <locale>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommNavigationObjects {
	
	class NavigationConnectonType {
	private:
		CommNavigationObjectsIDL::NavigationConnectonType value;
		
	public:
		enum ENUM_NavigationConnectonType {
			ENUM_VALUE_UNDEFINED = 0,
			VIA_LOCATION = 1
		};
		
		// default constructor
		NavigationConnectonType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		NavigationConnectonType(ENUM_NavigationConnectonType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type (which is typically int)
		NavigationConnectonType(CommNavigationObjectsIDL::NavigationConnectonType e) {
			value = e;
		}
		
		//const CommNavigationObjectsIDL::NavigationConnectonType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_NavigationConnectonType() const {
			return static_cast<ENUM_NavigationConnectonType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_NavigationConnectonType t) const {
			return this->value == t;
		}
		
		std::string to_string(const bool &use_fqn=true) const {
			std::string result = "";
			if(use_fqn == true) {
				result = "NavigationConnectonType::";
			}
			switch (value) {
				case VIA_LOCATION:
					result += "VIA_LOCATION";
					break;
				default:
					result += "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		static NavigationConnectonType from_string(const std::string &value) {
			std::string input = value;
			std::locale l;
			for(auto &c: input) {
				// convert all characters to lower case (so string comparison works regardless of small/capital letters)
				c = std::tolower(c,l);
			}
			std::string base_name = "navigationconnectontype::";
			if(input.compare(0, base_name.length(), base_name) == 0) {
				// remove basename from comparing the actual enumeration
				input.erase(0,base_name.length());
			}
			if(input == "via_location"){
				return NavigationConnectonType(VIA_LOCATION);
			}
			// default (if none of the preceding options match)
			return NavigationConnectonType();
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const NavigationConnectonType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
