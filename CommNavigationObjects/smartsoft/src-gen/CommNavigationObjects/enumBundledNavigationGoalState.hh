//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMNAVIGATIONOBJECTS_BUNDLEDNAVIGATIONGOALSTATE_ENUM_H_
#define COMMNAVIGATIONOBJECTS_BUNDLEDNAVIGATIONGOALSTATE_ENUM_H_

#include <CommNavigationObjects/enumBundledNavigationGoalStateData.hh>

#include <string>
#include <iostream>
#include <locale>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommNavigationObjects {
	
	class BundledNavigationGoalState {
	private:
		CommNavigationObjectsIDL::BundledNavigationGoalState value;
		
	public:
		enum ENUM_BundledNavigationGoalState {
			ENUM_VALUE_UNDEFINED = 0,
			GOAL_UNDEFINED = 1,
			GOAL_REACHED = 2,
			GOAL_CANCELED = 3,
			GOAL_NOT_REACHABLE = 4
		};
		
		// default constructor
		BundledNavigationGoalState() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		BundledNavigationGoalState(ENUM_BundledNavigationGoalState e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type (which is typically int)
		BundledNavigationGoalState(CommNavigationObjectsIDL::BundledNavigationGoalState e) {
			value = e;
		}
		
		//const CommNavigationObjectsIDL::BundledNavigationGoalState& get() const { return value; }
		
		// ENUM operator
		operator ENUM_BundledNavigationGoalState() const {
			return static_cast<ENUM_BundledNavigationGoalState>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_BundledNavigationGoalState t) const {
			return this->value == t;
		}
		
		std::string to_string(const bool &use_fqn=true) const {
			std::string result = "";
			if(use_fqn == true) {
				result = "BundledNavigationGoalState::";
			}
			switch (value) {
				case GOAL_UNDEFINED:
					result += "GOAL_UNDEFINED";
					break;
				case GOAL_REACHED:
					result += "GOAL_REACHED";
					break;
				case GOAL_CANCELED:
					result += "GOAL_CANCELED";
					break;
				case GOAL_NOT_REACHABLE:
					result += "GOAL_NOT_REACHABLE";
					break;
				default:
					result += "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		static BundledNavigationGoalState from_string(const std::string &value) {
			std::string input = value;
			std::locale l;
			for(auto &c: input) {
				// convert all characters to lower case (so string comparison works regardless of small/capital letters)
				c = std::tolower(c,l);
			}
			std::string base_name = "bundlednavigationgoalstate::";
			if(input.compare(0, base_name.length(), base_name) == 0) {
				// remove basename from comparing the actual enumeration
				input.erase(0,base_name.length());
			}
			if(input == "goal_undefined"){
				return BundledNavigationGoalState(GOAL_UNDEFINED);
			}
			if(input == "goal_reached"){
				return BundledNavigationGoalState(GOAL_REACHED);
			}
			if(input == "goal_canceled"){
				return BundledNavigationGoalState(GOAL_CANCELED);
			}
			if(input == "goal_not_reachable"){
				return BundledNavigationGoalState(GOAL_NOT_REACHABLE);
			}
			// default (if none of the preceding options match)
			return BundledNavigationGoalState();
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const BundledNavigationGoalState &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
