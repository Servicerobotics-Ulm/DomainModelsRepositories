//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMNAVIGATIONOBJECTS_PATHNAVIGATIONEVENTTYPE_ENUM_H_
#define COMMNAVIGATIONOBJECTS_PATHNAVIGATIONEVENTTYPE_ENUM_H_

#include <CommNavigationObjects/enumPathNavigationEventTypeData.hh>

#include <string>
#include <iostream>
#include <locale>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommNavigationObjects {
	
	class PathNavigationEventType {
	private:
		CommNavigationObjectsIDL::PathNavigationEventType value;
		
	public:
		enum ENUM_PathNavigationEventType {
			ENUM_VALUE_UNDEFINED = 0,
			GOAL_UNKNOWN = 1,
			START_TOO_FAR = 2,
			GOAL_REACHED = 3,
			GOAL_NOT_REACHED = 4,
			NO_PATH_FOUND = 5,
			FATAL_ERROR = 6
		};
		
		// default constructor
		PathNavigationEventType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		PathNavigationEventType(ENUM_PathNavigationEventType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type (which is typically int)
		PathNavigationEventType(CommNavigationObjectsIDL::PathNavigationEventType e) {
			value = e;
		}
		
		//const CommNavigationObjectsIDL::PathNavigationEventType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_PathNavigationEventType() const {
			return static_cast<ENUM_PathNavigationEventType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_PathNavigationEventType t) const {
			return this->value == t;
		}
		
		std::string to_string(const bool &use_fqn=true) const {
			std::string result = "";
			if(use_fqn == true) {
				result = "PathNavigationEventType::";
			}
			switch (value) {
				case GOAL_UNKNOWN:
					result += "GOAL_UNKNOWN";
					break;
				case START_TOO_FAR:
					result += "START_TOO_FAR";
					break;
				case GOAL_REACHED:
					result += "GOAL_REACHED";
					break;
				case GOAL_NOT_REACHED:
					result += "GOAL_NOT_REACHED";
					break;
				case NO_PATH_FOUND:
					result += "NO_PATH_FOUND";
					break;
				case FATAL_ERROR:
					result += "FATAL_ERROR";
					break;
				default:
					result += "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		static PathNavigationEventType from_string(const std::string &value) {
			std::string input = value;
			std::locale l;
			for(auto &c: input) {
				// convert all characters to lower case (so string comparison works regardless of small/capital letters)
				c = std::tolower(c,l);
			}
			std::string base_name = "pathnavigationeventtype::";
			if(input.compare(0, base_name.length(), base_name) == 0) {
				// remove basename from comparing the actual enumeration
				input.erase(0,base_name.length());
			}
			if(input == "goal_unknown"){
				return PathNavigationEventType(GOAL_UNKNOWN);
			}
			if(input == "start_too_far"){
				return PathNavigationEventType(START_TOO_FAR);
			}
			if(input == "goal_reached"){
				return PathNavigationEventType(GOAL_REACHED);
			}
			if(input == "goal_not_reached"){
				return PathNavigationEventType(GOAL_NOT_REACHED);
			}
			if(input == "no_path_found"){
				return PathNavigationEventType(NO_PATH_FOUND);
			}
			if(input == "fatal_error"){
				return PathNavigationEventType(FATAL_ERROR);
			}
			// default (if none of the preceding options match)
			return PathNavigationEventType();
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const PathNavigationEventType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
