//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef COMMNAVIGATIONOBJECTS_DOCKINGEVENTTYPE_ENUM_H_
#define COMMNAVIGATIONOBJECTS_DOCKINGEVENTTYPE_ENUM_H_

#include <CommNavigationObjects/enumDockingEventTypeData.hh>

#include <string>
#include <iostream>
#include <locale>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace CommNavigationObjects {
	
	class DockingEventType {
	private:
		CommNavigationObjectsIDL::DockingEventType value;
		
	public:
		enum ENUM_DockingEventType {
			ENUM_VALUE_UNDEFINED = 0,
			DOCKING_UNKNOWN = 1,
			DOCKING_DONE = 2,
			DOCKING_NOT_DONE = 3,
			DOCKING_ERROR = 4,
			UN_DOCKING_NOT_DONE = 5,
			UN_DOCKING_DONE = 6,
			UN_DOCKING_ERROR = 7
		};
		
		// default constructor
		DockingEventType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		DockingEventType(ENUM_DockingEventType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type (which is typically int)
		DockingEventType(CommNavigationObjectsIDL::DockingEventType e) {
			value = e;
		}
		
		//const CommNavigationObjectsIDL::DockingEventType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_DockingEventType() const {
			return static_cast<ENUM_DockingEventType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_DockingEventType t) const {
			return this->value == t;
		}
		
		std::string to_string(const bool &use_fqn=true) const {
			std::string result = "";
			if(use_fqn == true) {
				result = "DockingEventType::";
			}
			switch (value) {
				case DOCKING_UNKNOWN:
					result += "DOCKING_UNKNOWN";
					break;
				case DOCKING_DONE:
					result += "DOCKING_DONE";
					break;
				case DOCKING_NOT_DONE:
					result += "DOCKING_NOT_DONE";
					break;
				case DOCKING_ERROR:
					result += "DOCKING_ERROR";
					break;
				case UN_DOCKING_NOT_DONE:
					result += "UN_DOCKING_NOT_DONE";
					break;
				case UN_DOCKING_DONE:
					result += "UN_DOCKING_DONE";
					break;
				case UN_DOCKING_ERROR:
					result += "UN_DOCKING_ERROR";
					break;
				default:
					result += "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		static DockingEventType from_string(const std::string &value) {
			std::string input = value;
			std::locale l;
			for(auto &c: input) {
				// convert all characters to lower case (so string comparison works regardless of small/capital letters)
				c = std::tolower(c,l);
			}
			std::string base_name = "dockingeventtype::";
			if(input.compare(0, base_name.length(), base_name) == 0) {
				// remove basename from comparing the actual enumeration
				input.erase(0,base_name.length());
			}
			if(input == "docking_unknown"){
				return DockingEventType(DOCKING_UNKNOWN);
			}
			if(input == "docking_done"){
				return DockingEventType(DOCKING_DONE);
			}
			if(input == "docking_not_done"){
				return DockingEventType(DOCKING_NOT_DONE);
			}
			if(input == "docking_error"){
				return DockingEventType(DOCKING_ERROR);
			}
			if(input == "un_docking_not_done"){
				return DockingEventType(UN_DOCKING_NOT_DONE);
			}
			if(input == "un_docking_done"){
				return DockingEventType(UN_DOCKING_DONE);
			}
			if(input == "un_docking_error"){
				return DockingEventType(UN_DOCKING_ERROR);
			}
			// default (if none of the preceding options match)
			return DockingEventType();
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const DockingEventType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
