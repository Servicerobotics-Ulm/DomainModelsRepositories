//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef DOMAINHMI_REMOTECONTROLEVENTTYPE_ENUM_H_
#define DOMAINHMI_REMOTECONTROLEVENTTYPE_ENUM_H_

#include <DomainHMI/enumRemoteControlEventTypeData.hh>

#include <string>
#include <iostream>
#include <locale>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace DomainHMI {
	
	class RemoteControlEventType {
	private:
		DomainHMIIDL::RemoteControlEventType value;
		
	public:
		enum ENUM_RemoteControlEventType {
			ENUM_VALUE_UNDEFINED = 0,
			SUCCESS = 1,
			FAILED = 2,
			INVOKE = 3
		};
		
		// default constructor
		RemoteControlEventType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		RemoteControlEventType(ENUM_RemoteControlEventType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type (which is typically int)
		RemoteControlEventType(DomainHMIIDL::RemoteControlEventType e) {
			value = e;
		}
		
		//const DomainHMIIDL::RemoteControlEventType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_RemoteControlEventType() const {
			return static_cast<ENUM_RemoteControlEventType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_RemoteControlEventType t) const {
			return this->value == t;
		}
		
		std::string to_string(const bool &use_fqn=true) const {
			std::string result = "";
			if(use_fqn == true) {
				result = "RemoteControlEventType::";
			}
			switch (value) {
				case SUCCESS:
					result += "SUCCESS";
					break;
				case FAILED:
					result += "FAILED";
					break;
				case INVOKE:
					result += "INVOKE";
					break;
				default:
					result += "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		static RemoteControlEventType from_string(const std::string &value) {
			std::string input = value;
			std::locale l;
			for(auto &c: input) {
				// convert all characters to lower case (so string comparison works regardless of small/capital letters)
				c = std::tolower(c,l);
			}
			std::string base_name = "remotecontroleventtype::";
			if(input.compare(0, base_name.length(), base_name) == 0) {
				// remove basename from comparing the actual enumeration
				input.erase(0,base_name.length());
			}
			if(input == "success"){
				return RemoteControlEventType(SUCCESS);
			}
			if(input == "failed"){
				return RemoteControlEventType(FAILED);
			}
			if(input == "invoke"){
				return RemoteControlEventType(INVOKE);
			}
			// default (if none of the preceding options match)
			return RemoteControlEventType();
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const RemoteControlEventType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
